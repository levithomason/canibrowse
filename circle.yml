version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.2.1
    working_directory: ~/repo
    steps:
      - checkout
      - run: yarn cache clean
      - restore_cache:
          keys:
          - v1-dependencies-{{ checksum "package.json" }}
          # fallback to using the latest cache if no exact match is found
          - v1-dependencies-
      - run: yarn install
      - save_cache:
          paths:
            - node_modules
            - ~/.cache/yarn
          key: v1-dependencies-{{ checksum "package.json" }}
      - run: yarn lint
      - run: yarn test

# deployment:
#   staging:
#     branch: master
#     commands:
#       - URL=$(now -t ${NOW_TOKEN})
#       - now -t ${NOW_TOKEN} alias set ${URL} canibrowse.now.sh